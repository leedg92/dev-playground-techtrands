# =============================================================================
# Apache Airflow 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요: cp env_sample.txt .env
# =============================================================================

# -----------------------------------------------------------------------------
# 필수 설정
# -----------------------------------------------------------------------------
# macOS/Linux 권한 문제 해결용 (필수!)
AIRFLOW_UID=50000

# -----------------------------------------------------------------------------
# 권장 설정
# -----------------------------------------------------------------------------
# Airflow 웹 UI 로그인 정보
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin123!

# 추가 Python 패키지는 requirements.txt와 Dockerfile에서 관리됩니다

# -----------------------------------------------------------------------------
# 선택 사항 (기본값이 있음)
# -----------------------------------------------------------------------------
# Airflow 프로젝트 디렉토리 (기본값: 현재 디렉토리)
# AIRFLOW_PROJ_DIR=.

# Airflow 이미지 (기본값: apache/airflow:2.11.0) 
# AIRFLOW_IMAGE_NAME=apache/airflow:2.11.0

# -----------------------------------------------------------------------------
# Airflow 2.11.0 최적화 설정
# -----------------------------------------------------------------------------
# 로깅 레벨 (DEBUG, INFO, WARNING, ERROR)
# AIRFLOW__LOGGING__LOGGING_LEVEL=INFO

# 웹서버 설정
# AIRFLOW__WEBSERVER__EXPOSE_CONFIG=true 